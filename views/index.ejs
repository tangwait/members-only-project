<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Homepage</title>
</head>
<body>
    <% if (locals.user) {%>
        <%- include('partials/memberMessages', { messages: messages }) %> 

    <% } else { %>
        <%- include('partials/anonMessages', { messages: messages }) %>

    <%}%>
</body>
</html>